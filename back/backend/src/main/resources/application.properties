spring.application.name=backend

#server.ssl.enabled=true
#server.ssl.key-store=${SPRING_PATH_KEYSTORE}
#server.ssl.key-store-password=${SPRING_PASSWORD_KEYSTORE}
#server.ssl.key-store-type=PKCS12

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
db.host=${DB_HOST:localhost}
db.port=${DB_PORT:3306}
db.name=${DB_NAME:cpmanager}
db.username=${DB_USERNAME:root}
db.password=${DB_PASSWORD:software.getsai.06}

server.port=${SERVER_PORT:8080}
server.servlet.context-path=/api

# Paginación: tope de tamaño de página para listados (Usuarios, Áreas, Clientes, Prendas, Actividades)
spring.data.web.pageable.max-page-size=100
spring.data.web.pageable.one-indexed-parameters=false

jwt.secret=${JWT_SECRET:D3V-JWT-S3CR37-D3V-JWT-S3CR37-2026}
jwt.expiration-ms=${JWT_EXPIRATION_MS:28800000}

# Semilla de administrador (creado automáticamente al arrancar si no existe)
app.admin-seed.usuario=${ADMIN_SEED_USUARIO:admin}
app.admin-seed.password=${ADMIN_SEED_PASSWORD:12345a}

# Recuperación de contraseña (código OTP)
app.otp.expiration-minutes=${OTP_EXPIRATION_MINUTES:10}
app.otp.max-attempts=${OTP_MAX_ATTEMPTS:5}

# EMAIL CONFIGURATION - SMTP_PASSWORD debe sobreescribirse por variable de entorno en producción
spring.mail.host=${SMTP_HOST:smtp.gmail.com}
spring.mail.port=${SMTP_PORT:587}
spring.mail.username=${SMTP_USERNAME:sotomorasenior@gmail.com}
spring.mail.password=${SMTP_PASSWORD:gshdpzcjycuunavy}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true