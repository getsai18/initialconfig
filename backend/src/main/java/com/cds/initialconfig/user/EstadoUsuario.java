package com.cds.initialconfig.user;

/** Nombres en minúscula a propósito: coinciden literalmente con "activo"/"inactivo"
 *  que usa el frontend (JSON directo, sin mapear). */
public enum EstadoUsuario {
    activo,
    inactivo
}
