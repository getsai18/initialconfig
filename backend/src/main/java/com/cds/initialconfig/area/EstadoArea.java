package com.cds.initialconfig.area;

/** Nombres de constante en minúscula a propósito: así Jackson serializa/deserializa
 *  exactamente los literales "activa"/"inactiva" que usa el frontend. */
public enum EstadoArea {
    activa,
    inactiva
}
