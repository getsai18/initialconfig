package com.cds.initialconfig.activity;

/** Nombres en minúscula a propósito: coinciden literalmente con los valores
 *  'radio' | 'checkbox' | 'texto' que usa ActivitiesPage.jsx. */
public enum TipoActividad {
    radio,
    checkbox,
    texto
}
